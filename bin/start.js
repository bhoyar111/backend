/**
 * Starts the backend server by importing:
 * - Server.js → sets up the HTTP server
 * - app.js → configures the Express app
 *
 * These imports initialize and run the server.
 */
import "../Server.js";
import "../app.js";
